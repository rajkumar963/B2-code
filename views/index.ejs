<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora Posts</h1>
    <% for(post of posts) {%>
        <div class="post">
            <h2 class="username">@<%= post.username %></h2>
            <h3 class="content">@<%= post.content %></h3><br>
            <a href="http://localhost:8080/posts/<%= post.id %>">Sea in Detail</a>
            <a href="http://localhost:8080/posts/<%= post.id %>/edit">Edit</a>
            <br>
            <form method="post" action="/posts/<%= post.id %>?_mthod=DELETE">
            <button>Delete post</button>
        </form>
        </div>
    <% } %>
    <br>
    <br>
    <a href="http://localhost:8080/posts/new">Create new post</a>
</body>
</html>